# Thread-Synchronization
This repo is for a class project for learning about thread/process synchronization.

# To run:
    1. `gcc proj3.c buffer.c -lpthread -o proj3`
    2. `./proj3 5 1 3 3 yes` You may change the parameters after the file name

